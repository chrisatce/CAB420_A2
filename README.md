# CAB420_A2
Repo for CAB420 group assignment

Clone this repo, then when you have your directory in place store the local copy of the dataset in the aeroscapes directory.  This will not be uploaded to github (we have a gitignore on the directory) so it can be kept in a common place across our environments without needing to send and receive GB's of data. 

I have not set up a directory structure, we can do that next week maybe if we see the need. Otherwise I suggest we just have code in the root directory for simplicity:
preprocessing
4 - different transformers
postprocessing

documentation and report